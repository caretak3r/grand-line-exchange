name: Update Prices

on:
  schedule:
    # Every 6 hours at :15 past (avoids the top-of-hour rush on GH runners)
    - cron: '15 */1 * * *'
  workflow_dispatch:    # allows manual trigger from the Actions tab
    inputs:
      dry_run:
        description: 'Run without committing changes'
        required: false
        default: 'false'

permissions:
  contents: write       # needed to commit updated JSON back to the repo

concurrency:
  group: update-prices
  cancel-in-progress: false

jobs:
  fetch-and-commit:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run price scraper
        env:
          EBAY_APP_ID: ${{ secrets.EBAY_APP_ID }}
          DRY_RUN: ${{ github.event.inputs.dry_run == 'true' && '1' || '0' }}
        run: python scripts/update-prices.py

      - name: Validate market data
        if: github.event.inputs.dry_run != 'true'
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          market = json.loads(Path("public/data/market.json").read_text())
          history = json.loads(Path("public/data/history.json").read_text())
          quotes = market.get("quotes", {})
          positive_quotes = sum(1 for q in quotes.values() if q.get("price", 0) > 0)
          nonempty_history = sum(1 for rows in history.values() if rows)

          if positive_quotes == 0:
              raise SystemExit("No positive market quotes found; refusing to commit empty data.")
          if nonempty_history == 0:
              raise SystemExit("No chart history found; refusing to commit empty data.")

          print(f"Validated {positive_quotes} positive quotes and {nonempty_history} history series.")
          PY

      - name: Commit and push if changed
        if: github.event.inputs.dry_run != 'true'
        run: |
          git config user.name  "grand-line-bot"
          git config user.email "grand-line-bot@users.noreply.github.com"
          git add public/data/market.json public/data/history.json public/data/transactions.json
          if git diff --staged --quiet; then
            echo "No price changes to commit."
          else
            git commit -m "📊 price update $(date -u +'%Y-%m-%d %H:%MZ')"
            git push
            echo "Committed price updates."
          fi
